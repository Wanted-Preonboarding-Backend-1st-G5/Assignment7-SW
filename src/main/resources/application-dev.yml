spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/cardoc_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

jwt:
  secret-key: cardoc-backend-1111

